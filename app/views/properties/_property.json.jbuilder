json.extract! property, :id, :property_id, :name, :address, :size, :features, :terms_available, :earliest_start_date, :application_fee, :monthly_rent, :occupied_by, :created_at, :updated_at
json.url property_url(property, format: :json)
